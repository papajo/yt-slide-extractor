version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    # Uncomment if you need to mount volumes for development
    # volumes:
    #   - .:/app
    #   - /app/node_modules
